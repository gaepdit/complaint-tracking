@model Cts.AppServices.Staff.StaffViewDto?
@if (Model is null)
{
    <span class="text-muted">[Not entered]</span>
}
else
{
    @Model.SortableFullName @if(!Model.Active)
    {
        <span class="text-muted">[Inactive]</span>
    }
    @if(Model.Office is not null)
    {
        <br />
        @Model.Office.Name
    }
}
