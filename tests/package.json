{
  "name": "cts-tests",
  "version": "1.0.0",
  "private": true,
  "description": "CTS Tests",
  "author": "Doug Waldron",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^6.6.0"
  },
  "scripts": {
    "cypress": "cypress open",
    "cypress:run": "cypress run",
    "cypress:dev": "cypress open --env configFile=dev",
    "cypress:dev-run": "cypress run --env configFile=dev",
    "cypress:uat": "cypress open --env configFile=uat",
    "cypress:uat-run": "cypress run --env configFile=uat",
    "cypress:prod": "cypress open --env configFile=prod",
    "cypress:prod-run": "cypress run --env configFile=prod"
  }
}
